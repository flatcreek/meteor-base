import styled from 'styled-components';

const VerifyEmailAlert = styled.div`
  .alert {
    border-top: none;
    border-radius: 0;
    padding: 0;

    p {
      padding: 19px;
    }

    .btn {
      padding: 0;
      text-decoration: underline;
      margin-left: 5px;
      margin-top: -2px;
    }
  }
`;

export default {
  VerifyEmailAlert,
};
